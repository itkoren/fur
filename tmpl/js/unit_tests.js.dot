{{
    /** @file Template for unit tests. */
}}/**
 * @namespace {{=it.root}}
 */

var path = require('path');

/**
 * Run a test.
 * @protected
 * @ignore
 */
exports._runTest = function (filename, callback) {

};

{{~it.namespaces :namespace}}
/**
 * @memberof {{=namespace.parent}}
 * @namespace {{=namespace.name}}
 */
exports.{{=namespace.keys}} = {};
{{~}}

{{~it.suites :suite}}
/**
 * @memberof {{=suite.parent}}
 * @namespace {{=suite.name}}
 */
exports.{{=suite.keys}} = [
    {{~suite.testcases :testcase}}
    /**
     * {{=testcase.name}}
     * @memberof {{=suite.parent}}.{{=suite.name}}
     * @function {{=testcase.name}}
     */
    '{{=testcase.name}}',
{{~}}
];
{{~}}
/**
 * @file nodeunit test case for "{{=it.moduleName}}}
 * @see {@link https://github.com/caolan/nodeunit | nodeunit}
 *
 */

var {{=it.moduleName}} = require('{{=it.relativePathToBaseDir}}/{{=it.modulePath}}'),
    testHelper = require('{{=it.relativePathToBaseDir}}/test/test_helper');

exports.setUp = function(done) {
    done();
};

exports.tearDown = function(done) {
    done();
};

exports['{{=it.testCaseName}}'] = function (test) {
    test.ifError(new Error('Not implemented!'));
    test.done();
};